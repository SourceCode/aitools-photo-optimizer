{
    "name": "demo-app",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "optimize": "apo build 'public/images/*.{jpg,png}' --out public/optimized --verbose"
    },
    "dependencies": {
        "@aitools-photo-optimizer/node": "workspace:*",
        "@aitools-photo-optimizer/web": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.9.3",
        "vite": "^7.3.1"
    }
}