# @aitools-photo-optimizer/core

Core logic for the Photo Optimizer tool. Contains types, interfaces, planning algorithms, and hashing utilities.

## Usage

```typescript
import { createTransformPlan, createContentHash } from '@aitools-photo-optimizer/core';

const plan = createTransformPlan(inputDescriptor, config);
```
